const { describe } = require('node:test');
const Shape = require('./shapes');

describe('Shape', ()=>{
    describe('circle', () => {
        it('should be centered and rounded with no straight sides',() =>{
            const shape = new Shape();
            expect(shape.//this is the part that makes it round
            );
        });
    });
});